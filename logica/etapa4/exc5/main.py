from mensagem import mensagem

if __name__ == "__main__": # garante que esse código só será executado quando o arquivo for rodado diretamente, e não se ele for importado por outro arquivo.
    mensagem()